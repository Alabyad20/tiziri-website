# Optional NEURAL tier (SAM 2 primary segmenter). Heavy; install only when you want
# SAM 2 quality. The classical pipeline (requirements.txt) runs without any of this.
# Install torch/torchvision from the CPU wheel index to avoid a large CUDA download:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-sam2.txt
#
# Then fetch the pinned Apache-2.0 checkpoint:
#   python scripts/download_models.py sam2_hiera_tiny

torch==2.5.1              # BSD-3-Clause
torchvision==0.20.1      # BSD-3-Clause
sam2==1.1.0              # Apache-2.0 (facebookresearch/sam2)
hydra-core==1.3.4        # MIT (sam2 dependency)
iopath==0.1.10           # MIT (sam2 dependency)

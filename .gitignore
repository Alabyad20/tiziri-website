# Claude Code (local tool settings — not project code)
.claude/
.agents/

# OS artifacts
.DS_Store
Thumbs.db
desktop.ini

# Editor artifacts
.vscode/
.idea/
*.suo
*.user

# Node (in case a build tool or package manager is added later)
node_modules/
npm-debug.log*

# Temp files
*.tmp
*.log

# Local admin tool — not part of the website
admin.js
admin.html

# Video files — too large for GitHub (use external storage instead)
*.mp4
*.mov
*.avi
*.mkv

# Rug Design Studio — standalone app, not part of this site's deploy
rug-designer/

# Logo QA/dev artifacts — not part of the live site, were being deployed and crawled
logos/check-*.png
logos/capture*.html
logos/preview.html
logos/weave-preview.html
logos/convert.js

# Internal planning/design artifacts — not part of the live site, were being
# deployed and publicly crawlable (brand strategy, launch checklist, mockups)
docs/
designs/
inspiration/
